{
  "name": "ghostcam-cloudflare-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute ghostcam --file=schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
